import Container from "@/Components/Container/Container";
import "./header.scss";

const Header = () => {
   return (
      <header className="header">
         <Container cn="header">
            Header
         </Container>
      </header>
   );
};

export default Header;
