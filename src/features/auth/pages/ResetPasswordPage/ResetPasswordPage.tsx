import "./reset-password-page.scss";

const ResetPasswordPage = () => {
   return (
      <section className="reset-password__page">reset password page</section>
   );
};

export default ResetPasswordPage;
